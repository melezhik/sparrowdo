
tasks:
  -
    name: test
    default: true
    language: Bash
    code: |
      tomty --all --show-failed --color 
    depends:
      -
        name: install-deps
  -
    name: install-deps
    language: Bash
    code: |
      zef install . --/test

