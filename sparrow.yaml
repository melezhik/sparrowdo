
tasks:
  -
    name: test
    default: true
    language: Bash
    code: |
      set -e
      cd source 
      tomty --all --show-failed --color 
    depends:
      -
        name: install-deps
  -
    name: install-deps
    language: Bash
    code: |
      set -e
      cd source 
      zef install . --/test

