bash "pwd", %(
  user => config()<user>,
  envvars => %( 
    PATH => "/home/{config()<user>}/.raku/bin:/home/{config()<user>}/.perl6/bin:{%*ENV<PATH>}",
    SP6_REPO => "http://repo.westus.cloudapp.azure.com"
  )
)
