bash "tomty -all --show-failed", %(
  user => config()<user>,
  envvars => %( 
    PATH => "/home/{config()<user>}/.raku/bin:{%*ENV<PATH>}",
    SP6_REPO => "http://repo.westus.cloudapp.azure.com"
  )
)
